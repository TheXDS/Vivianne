<UserControl
    x:Class="TheXDS.Vivianne.Views.Asf.MusPlayerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:asf="clr-namespace:TheXDS.Vivianne.ViewModels.Asf;assembly=Vivianne.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hlp="clr-namespace:TheXDS.Vivianne.Helpers"
    xmlns:local="clr-namespace:TheXDS.Vivianne.Views.Asf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vc="clr-namespace:TheXDS.Vivianne.ValueConverters"
    xmlns:vm="clr-namespace:TheXDS.Vivianne.ViewModels.Bnk;assembly=Vivianne.Common"
    d:DataContext="{d:DesignInstance Type=asf:MusPlayerViewModel}"
    d:DesignWidth="500"
    hlp:Form.BetaIndicator="True"
    hlp:Form.BetaMessage="This feature is in early stages of development."
    mc:Ignorable="d">
    <UserControl.Resources>
        <vc:MusStreamInformationConverter x:Key="mus2nfo" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <DockPanel>
                <ui:Button
                    Margin="5"
                    Command="{Binding PlaySampleCommand, Mode=OneWay}"
                    Icon="{ui:SymbolIcon Symbol=Play24}" />
                <ui:Button
                    Margin="5"
                    Command="{Binding StopPlaybackCommand, Mode=OneWay}"
                    Icon="{ui:SymbolIcon Symbol=Stop24}" />
                <ui:Button
                    Margin="5"
                    Command="{Binding ExportAudioCommand, Mode=OneWay}"
                    DockPanel.Dock="Right"
                    Icon="{ui:SymbolIcon Symbol=Save24}" />
                <TextBlock Margin="10,0" VerticalAlignment="Center">
                    <Run Text="{Binding CurrentPosition, Mode=OneWay, StringFormat='mm\\:ss'}" />
                    |&#160;<Run Text="{Binding Duration, Mode=OneWay, StringFormat='mm\\:ss'}" />
                </TextBlock>
            </DockPanel>
            <ui:ToggleSwitch
                Margin="5"
                Content="Play looping"
                IsChecked="{Binding PlayLooping}"
                IsEnabled="{Binding CanPlayLooping, Mode=OneWay}" />
            <TextBlock Visibility="{Binding AudioFile, Converter={StaticResource NotNull2VisConv}}">
                Current substream block:&#160;<Run Text="{Binding AudioFile.CurrentAsfSubStreamIndexForMap, Mode=OneWay}" />
            </TextBlock>
        </StackPanel>
        <TextBlock
            Grid.Column="1"
            Margin="10"
            Padding="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="#40000000"
            Text="{Binding Mus, Mode=OneWay, Converter={StaticResource mus2nfo}}"
            TextWrapping="Wrap" />
    </Grid>
</UserControl>
