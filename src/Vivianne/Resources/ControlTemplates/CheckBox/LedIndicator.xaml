<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TheXDS.Vivianne.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Style x:Key="LedIndicator" TargetType="{x:Type CheckBox}">
        <Setter Property="IsEnabled" Value="False" />
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <DockPanel
                        x:Name="templateRoot"
                        Background="Transparent"
                        SnapsToDevicePixels="True">
                        <DockPanel.Resources>
                            <Color x:Key="ledOnColor">#00ff00</Color>
                            <Color x:Key="ledOffColor">#206020</Color>
                            <Color x:Key="shadowColor">#404040</Color>
                        </DockPanel.Resources>
                        <Grid
                            Width="6"
                            Height="6"
                            Margin="10,5"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            DockPanel.Dock="Left">
                            <Ellipse x:Name="redLed">
                                <Ellipse.Stroke>
                                    <LinearGradientBrush EndPoint="0,1">
                                        <GradientStop Color="{ui:ThemeResource SystemAccentColorTertiary}" />
                                        <GradientStop Offset="1" Color="{DynamicResource shadowColor}" />
                                    </LinearGradientBrush>
                                </Ellipse.Stroke>
                                <Ellipse.Fill>
                                    <LinearGradientBrush EndPoint="0,1">
                                        <GradientStop Color="White" />
                                        <GradientStop Offset="0.5" Color="{ui:ThemeResource SystemAccentColorTertiary}" />
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <Ellipse x:Name="greenLed" Visibility="Collapsed">
                                <Ellipse.Stroke>
                                    <LinearGradientBrush EndPoint="0,1">
                                        <GradientStop Offset="0.5" Color="{ui:ThemeResource SystemAccentColor}" />
                                        <GradientStop Offset="1" Color="{DynamicResource shadowColor}" />
                                    </LinearGradientBrush>
                                </Ellipse.Stroke>
                                <Ellipse.Fill>
                                    <LinearGradientBrush EndPoint="0,1">
                                        <GradientStop Color="White" />
                                        <GradientStop Offset="0.5" Color="{ui:ThemeResource SystemAccentColor}" />
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                                <Ellipse.Effect>
                                    <DropShadowEffect
                                        BlurRadius="10"
                                        ShadowDepth="0"
                                        Color="{ui:ThemeResource SystemAccentColor}" />
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </DockPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="redLed" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="greenLed" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>