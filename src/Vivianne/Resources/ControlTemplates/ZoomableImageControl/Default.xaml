<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TheXDS.Vivianne.Controls">
    <Style TargetType="{x:Type local:ZoomableImageControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ZoomableImageControl}">
                    <Grid ClipToBounds="True">
                        <Border x:Name="PART_Container" Background="{TemplateBinding Background}">
                            <Image
                                x:Name="PART_Image"
                                RenderTransformOrigin="0,0"
                                Source="{TemplateBinding Source}"
                                Stretch="None" />
                        </Border>
                        <TextBlock
                            Margin="8"
                            Padding="4"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Background="#80000000"
                            Foreground="White"
                            Text="{TemplateBinding CoordinateText}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>